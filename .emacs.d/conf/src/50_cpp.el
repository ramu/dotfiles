;==========================
; programming language c++
;==========================

(setenv "CPLUS_INCLUDE_PATH" (concat '"/opt/local/include/"
                                     (getenv "CPLUS_INCLUDE_PATH")))

;(add-hook 'c++-mode-hook
;          '(lambda()
;             ))
