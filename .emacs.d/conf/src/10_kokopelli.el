;=================================
; kokopelli.el(関数一覧表示）
;=================================
(require 'kokopelli)
(define-key global-map [f12] 'kokopelli-sing)

; 関数選んだら閉じる
(setq kokopelli-auto-quit t)

